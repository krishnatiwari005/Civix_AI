rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write access for preview and demo environments
    // where custom passwordless bypass auth is used
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
